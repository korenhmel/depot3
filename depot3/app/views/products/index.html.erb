<p id="notice"><%= notice %></p>


<table border="1" > 
  <thead>
    <tr>
      <th colspan= "4"><h1>Products</h1></th>
      <!-- <th>Description</th>
      <th>Image</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr> -->
  </thead>

  <tbody>
  <h1>YOUR PRAGMATIC CATALOG</h1>
  <% cache ['store', Product.latest] do %>
    <% @products.each do |product| %>
     <%# cache ['entry', product] do %>
      <tr class= "<%= cycle('list_line_odd', 'list_line_even') %>">
        <td width="10%"><%= image_tag(product.image_url, class: 'list_image') %></td>

          <td width="75%" class= "list_description">
           <dl>
             <dt><%= product.title %></dt>
             <dd><%= truncate(strip_tags(product.description), length: 90) %></dd>
            </dl>
          </td>

          <td width="10%" class= "list_actions">
           
        
          <!-- <td><%= product.price %></td> -->
          <%= link_to 'Show', product %> <br>
          <%= link_to 'Edit', edit_product_path(product) %> <br>
          <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %>
          </td>
         
          <td width="5%"><%= number_to_currency(product.price, unit: "₴")%>.грн</td>
        </tr>
    <% end %>
    <% end %>
    <%# end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product', new_product_path %>
